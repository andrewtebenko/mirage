document.addEventListener('DOMContentLoaded', () => {
    
    // const wrapperOfCards = document.querySelector('.services__cards'),
    //       cards = document.querySelectorAll('.services__cards-item'),
    //       link = document.querySelectorAll('.services__cards-more');
});